# Registers the containerd runtime k3s auto-configures when nvidia-container-toolkit
# is present on the host (see setup.sh) under the name "nvidia" — job manifests in this
# directory reference it via `runtimeClassName: nvidia`.
apiVersion: node.k8s.io/v1
kind: RuntimeClass
metadata:
  name: nvidia
handler: nvidia
